# Knowledge Engine (Member 3). Merge these into the repo's requirements.txt
# rather than replacing it - other members' pins must be preserved.
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6
networkx>=3.2
opencv-python-headless>=4.9
numpy>=1.26

# Test-only
pytest>=8.0
httpx>=0.27
jsonschema>=4.20
